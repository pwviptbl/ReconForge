# =============================================================================
# ARQUIVO DE EXEMPLO DE VARIÁVEIS DE AMBIENTE
# =============================================================================
# Copie este arquivo para .env e configure as variáveis conforme necessário
# Comando: cp .env.example .env
#
# IMPORTANTE: Nunca commite o arquivo .env no repositório!
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DA API GEMINI
# -----------------------------------------------------------------------------
# Chave da API do Google Gemini para análise inteligente
# Obtenha sua chave em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO OPENVAS/GVM
# -----------------------------------------------------------------------------
# Host do servidor OpenVAS/GVM (padrão: localhost)
OPENVAS_HOST=localhost
# Porta do servidor GVM (padrão: 9390)
OPENVAS_PORT=9390
# Usuário para autenticação no OpenVAS/GVM (padrão: admin)
OPENVAS_USERNAME=admin
# Senha para autenticação no OpenVAS/GVM (padrão: admin)
OPENVAS_PASSWORD=
# Caminho para o socket Unix do GVM (usado quando disponível)
OPENVAS_SOCKET=/run/gvmd/gvmd.sock

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE BANCO DE DADOS (OPCIONAL)
# -----------------------------------------------------------------------------
# Tipo de banco de dados (sqlite, mysql, postgresql)
DB_TYPE=sqlite
# Arquivo do banco SQLite (usado quando DB_TYPE=sqlite)
DB_FILE=dados/pentest.db
# Host do banco de dados (para MySQL/PostgreSQL)
DB_HOST=localhost
# Porta do banco de dados
DB_PORT=3306
# Nome do banco de dados
DB_NAME=pentest
# Usuário do banco de dados
DB_USER=
# Senha do banco de dados
DB_PASSWORD=

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE LOGGING
# -----------------------------------------------------------------------------
# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
# Diretório para arquivos de log
LOG_DIR=logs
# Tamanho máximo do arquivo de log em MB
LOG_MAX_SIZE=10
# Número de arquivos de backup de log
LOG_BACKUP_COUNT=5

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE RELATÓRIOS
# -----------------------------------------------------------------------------
# Diretório para salvar relatórios
REPORTS_DIR=relatorios
# Formato padrão dos relatórios (html, pdf, json, xml)
REPORTS_FORMAT=html
# Template padrão para relatórios
REPORTS_TEMPLATE=padrao

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE SEGURANÇA
# -----------------------------------------------------------------------------
# Timeout para operações em segundos (padrão: 600 = 10 minutos)
SECURITY_TIMEOUT=600
# Habilitar validação de entrada (true/false)
SECURITY_VALIDATE_INPUT=true
# Habilitar log de comandos executados (true/false)
SECURITY_LOG_COMMANDS=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO NMAP
# -----------------------------------------------------------------------------
# Caminho para o binário do Nmap (deixe vazio para usar o padrão do sistema)
NMAP_BINARY=nmap
# Timeout padrão para varreduras Nmap em segundos
NMAP_TIMEOUT=300
# Range de portas padrão para varreduras
NMAP_DEFAULT_PORTS=1-65535

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE PROXY (OPCIONAL)
# -----------------------------------------------------------------------------
# Proxy HTTP (formato: http://user:pass@host:port)
HTTP_PROXY=
# Proxy HTTPS (formato: https://user:pass@host:port)
HTTPS_PROXY=
# Hosts que devem ignorar o proxy (separados por vírgula)
NO_PROXY=localhost,127.0.0.1

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE DESENVOLVIMENTO
# -----------------------------------------------------------------------------
# Modo de desenvolvimento (true/false)
DEBUG_MODE=false
# Habilitar modo verboso (true/false)
VERBOSE_MODE=false
# Salvar dados de debug (true/false)
SAVE_DEBUG_DATA=false

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE NOTIFICAÇÕES (OPCIONAL)
# -----------------------------------------------------------------------------
# Email para notificações
NOTIFICATION_EMAIL=
# Servidor SMTP
SMTP_SERVER=
# Porta SMTP
SMTP_PORT=587
# Usuário SMTP
SMTP_USER=
# Senha SMTP
SMTP_PASSWORD=
# Usar TLS para SMTP (true/false)
SMTP_USE_TLS=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE INTEGRAÇÃO (OPCIONAL)
# -----------------------------------------------------------------------------
# Webhook URL para notificações
WEBHOOK_URL=
# Token de autenticação para webhook
WEBHOOK_TOKEN=
# Slack webhook URL
SLACK_WEBHOOK_URL=
# Discord webhook URL
DISCORD_WEBHOOK_URL=

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE CACHE
# -----------------------------------------------------------------------------
# Diretório para cache temporário
CACHE_DIR=cache
# Tempo de vida do cache em segundos (padrão: 3600 = 1 hora)
CACHE_TTL=3600
# Tamanho máximo do cache em MB
CACHE_MAX_SIZE=100

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE BACKUP
# -----------------------------------------------------------------------------
# Diretório para backups
BACKUP_DIR=backups
# Habilitar backup automático (true/false)
AUTO_BACKUP=true
# Intervalo de backup em horas
BACKUP_INTERVAL=24
# Número máximo de backups a manter
MAX_BACKUPS=7
